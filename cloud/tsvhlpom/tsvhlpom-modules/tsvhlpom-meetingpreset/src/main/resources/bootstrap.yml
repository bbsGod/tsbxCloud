# Tomcat
server:
  port: 18091

# Spring
spring:
  servlet:
    multipart:
      max-request-size: 100MB
      max-file-size: 50MB
  profiles:
    active: ${platenv:dev}
  application:
    # 应用名称
    name: tsvhlpom-meetingpreset
