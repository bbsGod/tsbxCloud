# Tomcat
server:
  port: 8080

# Spring
spring:
  application:
    # 应用名称
    name: tsvhlpom-gateway
  profiles:
    # 环境配置
    active: ${platnev:dev}
  main:
    allow-bean-definition-overriding: true
